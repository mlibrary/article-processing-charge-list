---
services:
  web:
    build:
      target: development
      context: .
    ports:
    - "3000:80"
    volumes:
    - ./html:/usr/local/apache2/htdocs

  rb:
    image: ruby:3.3
    volumes:
    - .:/usr/src/app
    command: "tail -f /dev/null"
    working_dir: /usr/src/app
    profiles: ["rb"]
